#!/usr/bin/expect
spawn /usr/local/{{package_name}}/bin/redis-cli  --cluster create --cluster-replicas {{replicas}} {% for host in play_hosts %}{{ host }}:{{ port }}{% if not loop.last %} {% endif %}{% endfor %}

expect "yes"
send "yes\r"

expect eof
